{
  "name": "@pagent/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pagent MCP server exposing exactly the read and write tools",
  "scripts": {
    "start": "node --experimental-strip-types server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
