{
  "name": "agent-ui-session",
  "version": "0.0.1",
  "private": true,
  "description": "Hosted UI rendering for terminal-bound AI agents — monorepo root",
  "workspaces": [
    "apps/api",
    "apps/web",
    "apps/mcp"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm -w @agent-ui-session/api run dev",
    "dev:web": "npm -w @agent-ui-session/web run dev",
    "build:web": "npm -w @agent-ui-session/web run build",
    "start:api": "npm -w @agent-ui-session/api run start",
    "smoke": "node apps/mcp/smoke.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
