{
  "name": "@agent-ui-session/mcp",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "MCP server exposing show_ui + wait_for_event tools for agent-ui-session",
  "bin": {
    "agent-ui-session-mcp": "server.ts"
  },
  "scripts": {
    "start": "node --experimental-strip-types server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.25.76"
  },
  "engines": {
    "node": ">=22"
  }
}
